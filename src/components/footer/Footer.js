import React from 'react';

const style = {
  main: {
    marginTop: 200,
    width: '100%',
    height: 400,
    backgroundColor: '#757575',
  }
}

const Footer = (props) => {
  return (
    <div style={style.main} className='container-footer'>

    </div>
  )
}

export default Footer;
